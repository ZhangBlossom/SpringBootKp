<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="zhang.blossom.seckillbyrocketmq.mapper.MessageIdempotentMapper">

    <select id="selectByUUID" resultType="zhang.blossom.seckillbyrocketmq.entity.MessageIdempotent">
        select * from message_idempotent where message_uuid = #{uuid}
    </select>
</mapper>

